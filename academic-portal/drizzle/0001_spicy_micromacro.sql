CREATE TABLE `activities` (
	`id` int AUTO_INCREMENT NOT NULL,
	`title` varchar(255) NOT NULL,
	`organizer` varchar(255) NOT NULL,
	`activityType` enum('workshop','seminar','competition','exhibition','other') NOT NULL DEFAULT 'workshop',
	`location` varchar(255) NOT NULL,
	`isOnline` boolean NOT NULL DEFAULT false,
	`onlineUrl` varchar(500),
	`description` text NOT NULL,
	`schedule` text,
	`maxParticipants` int,
	`registrationFee` int DEFAULT 0,
	`contactEmail` varchar(320),
	`contactPhone` varchar(20),
	`registrationUrl` varchar(500),
	`startDate` timestamp NOT NULL,
	`endDate` timestamp,
	`registrationDeadline` timestamp,
	`isActive` boolean NOT NULL DEFAULT true,
	`viewCount` int NOT NULL DEFAULT 0,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `activities_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `courses` (
	`id` int AUTO_INCREMENT NOT NULL,
	`code` varchar(20) NOT NULL,
	`title` varchar(255) NOT NULL,
	`titleEn` varchar(255),
	`credits` int NOT NULL DEFAULT 3,
	`courseType` enum('required','elective','general') NOT NULL DEFAULT 'elective',
	`level` enum('undergraduate','graduate','all') NOT NULL DEFAULT 'undergraduate',
	`description` text NOT NULL,
	`objectives` text,
	`syllabus` text,
	`prerequisites` text,
	`schedule` varchar(255),
	`room` varchar(100),
	`semester` varchar(20),
	`academicYear` varchar(10),
	`maxStudents` int,
	`instructorId` int,
	`isActive` boolean NOT NULL DEFAULT true,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `courses_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `instructors` (
	`id` int AUTO_INCREMENT NOT NULL,
	`name` varchar(255) NOT NULL,
	`title` varchar(100),
	`position` varchar(255),
	`department` varchar(255),
	`email` varchar(320),
	`phone` varchar(20),
	`office` varchar(255),
	`imageUrl` varchar(500),
	`bio` text,
	`education` text,
	`expertise` text,
	`publications` text,
	`isActive` boolean NOT NULL DEFAULT true,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `instructors_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `jobs` (
	`id` int AUTO_INCREMENT NOT NULL,
	`title` varchar(255) NOT NULL,
	`company` varchar(255) NOT NULL,
	`location` varchar(255) NOT NULL,
	`jobType` enum('fulltime','parttime','internship','contract') NOT NULL DEFAULT 'fulltime',
	`salaryMin` int,
	`salaryMax` int,
	`description` text NOT NULL,
	`requirements` text,
	`benefits` text,
	`contactEmail` varchar(320),
	`contactPhone` varchar(20),
	`applicationUrl` varchar(500),
	`deadline` timestamp,
	`isUrgent` boolean NOT NULL DEFAULT false,
	`isActive` boolean NOT NULL DEFAULT true,
	`viewCount` int NOT NULL DEFAULT 0,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `jobs_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `scholarships` (
	`id` int AUTO_INCREMENT NOT NULL,
	`title` varchar(255) NOT NULL,
	`provider` varchar(255) NOT NULL,
	`amount` int,
	`amountDescription` varchar(255),
	`scholarshipType` enum('full','partial','grant','loan') NOT NULL DEFAULT 'partial',
	`eligibility` text NOT NULL,
	`description` text NOT NULL,
	`requirements` text,
	`applicationProcess` text,
	`contactEmail` varchar(320),
	`contactPhone` varchar(20),
	`applicationUrl` varchar(500),
	`startDate` timestamp,
	`deadline` timestamp,
	`isActive` boolean NOT NULL DEFAULT true,
	`viewCount` int NOT NULL DEFAULT 0,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `scholarships_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
ALTER TABLE `courses` ADD CONSTRAINT `courses_instructorId_instructors_id_fk` FOREIGN KEY (`instructorId`) REFERENCES `instructors`(`id`) ON DELETE no action ON UPDATE no action;